# ADR-013：统一像素渲染标准

- 问题：如何避免像素密度/采样/缩放不统一导致画面破碎。
- 候选方案：各资源自行设置；逐场景调整；全局 Art Direction + 统一 Import/渲染规则。
- 最终方案：统一像素密度、Nearest 过滤、无 mipmap、Pixel Snap、整数缩放、统一 viewport。
- 选择原因：保证长期生产的一致性，防止“角色大小/颗粒不一致”。
- 优缺点：优点是全局一致；缺点是需所有资源遵守规格。
- 未来影响：所有 Sprite/Tileset/VFX 遵循 `docs/art/` 渲染规范。
