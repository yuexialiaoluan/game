# Encounter

`EncounterService`：World Encounter 解析（Avoided/Negotiated/Escaped/CombatStarted/Surrendered/Captured）；`EscapeService` 与 `CombatService.start_combat()` 接口预留。

## Combat
CombatStarted 通过 CombatService 创建 Battle Scene。
